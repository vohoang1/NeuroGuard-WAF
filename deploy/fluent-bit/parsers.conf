[PARSER]
    Name        envoy_wasm_json
    Format      regex
    # Matches the JSON object at the end of the Envoy wasm log output
    Regex       /(?<json>\{.*\})$/
    # Decode the extracted json object into fields
    Decode_Field_As json json
